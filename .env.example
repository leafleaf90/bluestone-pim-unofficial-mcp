PAPI_KEY=your-papi-key-here
MAPI_CLIENT_ID=your-mapi-client-id
MAPI_CLIENT_SECRET=your-mapi-client-secret

# Required for remote (Vercel) deployment only.
# Used to HMAC-sign auth codes and encrypt bearer tokens with AES-256-GCM.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SIGNING_SECRET=
